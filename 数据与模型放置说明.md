# 数据与模型放置说明

## 1. 总体原则

本附件不携带数据集与模型文件。运行测试前，请在附件根目录下补充 `dataset/` 和 `model/` 两个目录，并按本文约定放置公开数据集和本地模型文件。

默认目录结构如下：

```text
附件/
├── 01_向量组数据结构Python实现代码/
├── 02_向量组数据结构API文档/
├── 03_与向量数据库对比测试工具/
├── 04_测试分析报告/
├── dataset/
└── model/
```

## 2. 数据集放置位置

建议按以下路径组织：

```text
dataset/
├── caltech-101/
│   └── 101_ObjectCategories/
├── CUB_200_2011/
│   └── images/
├── coco/
│   ├── coco_dataset_10000.json
│   └── ...图像文件...
├── 20_newsgroups/
├── ohsumed-all-docs/
│   └── ohsumed-all/
└── Yahoo!-answer_processed/
```

各脚本默认读取：

- `dataset/caltech-101/101_ObjectCategories`
- `dataset/CUB_200_2011/images`
- `dataset/coco`
- `dataset/20_newsgroups`
- `dataset/ohsumed-all-docs/ohsumed-all`
- `dataset/Yahoo!-answer_processed`

## 3. 数据集来源与下载说明

| 数据集 | 下载来源 | 下载后放置位置 | 说明 |
| --- | --- | --- | --- |
| Caltech-101 | https://data.caltech.edu/records/20086 | `dataset/caltech-101/101_ObjectCategories/` | 下载 `caltech-101.zip` 后解压，保留 `101_ObjectCategories` 目录。 |
| CUB-200-2011 | https://www.vision.caltech.edu/datasets/cub_200_2011/ | `dataset/CUB_200_2011/images/` | 下载 `Images and annotations`，解压后保留 `images` 目录。 |
| COCO | https://cocodataset.org/#download | `dataset/coco/` | 当前 benchmark 默认读取 `dataset/coco/coco_dataset_10000.json`。本目录已提供 `benchmarks/coco/download_coco_dataset.py`，可从公开 COCO URL-Text 数据生成图片清单和本地图片。JSON 条目需包含 `path` 或 `file_name` 字段，并指向 `dataset/coco/` 下的图片文件。 |
| 20 Newsgroups | https://archive.ics.uci.edu/dataset/113/twenty+newsgroups | `dataset/20_newsgroups/` | 下载并解压 `20_newsgroups.tar.gz`，保持每个新闻组一个子目录的结构。 |
| OHSUMED | https://zenodo.org/records/5256849 | `dataset/ohsumed-all-docs/ohsumed-all/` | 下载 `texts.txt` 和 `score.txt` 后，需要按类别整理为子目录，每个类别目录下放置文本文件。benchmark 默认读取已经整理好的类别目录。 |
| Yahoo Answers | https://huggingface.co/datasets/yassiracharki/Yahoo_Answers_10_categories_for_NLP | `dataset/Yahoo!-answer_processed/` | 下载原始数据后，先放入 `dataset/Yahoo!-answer/`，再使用本文第 4 节脚本生成 `Yahoo!-answer_processed`。 |

## 4. Yahoo Answers 预处理

Yahoo Answers benchmark 使用按类别组织的纯文本目录，不直接读取原始 CSV。原始数据目录应包含：

```text
dataset/
└── Yahoo!-answer/
    ├── classes.txt
    ├── train.csv 或 train.xls
    └── test.csv 或 test.xls
```

在 `03_与向量数据库对比测试工具/` 目录下执行：

```bash
python benchmarks/yahoo/organize_yahoo_answer.py
```

脚本会生成：

```text
dataset/
└── Yahoo!-answer_processed/
    ├── Society & Culture/
    ├── Science & Mathematics/
    ├── Health/
    └── ...其他类别目录...
```

如数据目录名称不同，可通过参数指定：

```bash
python benchmarks/yahoo/organize_yahoo_answer.py \
  --dataset-dir ../dataset \
  --yahoo-dir Yahoo!-answer \
  --output-dir ../dataset/Yahoo!-answer_processed
```

## 5. COCO 数据准备

COCO benchmark 默认读取：

```text
dataset/
└── coco/
    ├── coco_dataset_10000.json
    └── images/
        ├── image_00000.jpg
        ├── image_00001.jpg
        └── ...
```

如需重新生成该目录，可在 `03_与向量数据库对比测试工具/` 目录下执行：

```bash
python benchmarks/coco/download_coco_dataset.py
```

默认下载最多 `10000` 张图片，并生成 `dataset/coco/coco_dataset_10000.json`。如需调整数量或输出位置，可使用：

```bash
python benchmarks/coco/download_coco_dataset.py \
  --output-dir ../dataset/coco \
  --max-items 10000 \
  --json-name coco_dataset_10000.json
```

## 6. 模型放置位置

### 6.1 图像 benchmark

图像 benchmark 使用 CLIP 提取特征。建议将本地权重放在：

```text
model/clip/ViT-B-32.pt
```

脚本默认读取环境变量：

- `CLIP_MODEL_PATH`：本地 CLIP 权重路径；
- `CLIP_MODEL_NAME`：模型名称，默认 `ViT-B/32`；
- `CLIP_DOWNLOAD_ROOT`：可选，指定缓存目录。

如果 `CLIP_MODEL_PATH` 已设置且文件存在，将优先使用本地模型。

### 6.2 文本 benchmark

当前文本 benchmark 默认使用 `tfidf`，即：

- 数据集到位即可运行；
- 不强依赖外部文本语义模型。

如需切换到本地语义模型，可将模型放在：

```text
model/sentence-transformers/all-MiniLM-L6-v2/
```

后续可在对应 benchmark 中把 `vectorizer_method` 切换为语义模型方式，并传入本地模型路径。

## 7. 直接运行方式

在附件目录下，补齐数据和模型后，进入 `03_与向量数据库对比测试工具/` 执行：

```bash
bash scripts/run_bench_vision.sh
bash scripts/run_bench_text.sh
```

如果使用默认目录结构，通常不需要额外改代码。只需保证：

- `01_向量组数据结构Python实现代码/semanticvg` 目录存在；
- `dataset/` 路径结构正确；
- 图像 benchmark 所需的 CLIP 模型可通过本地路径或缓存加载。
